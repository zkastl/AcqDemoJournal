<UserControl x:Class="AcqDemoJournal.Controls.CriEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical" Name="CriEntryRoot">
        <Label Content="{Binding CriLabelText}" 
               FontSize="16" 
               FontWeight="Bold" 
               Foreground="#FFDEDEDE" />
        <TextBox Height="150" 
                 Background="#FFDEDEDE" 
                 TextWrapping="Wrap"
                 FontSize="16"
                 FontFamily="Courier New"
                 FontWeight="Bold"
                 Text="{Binding CriContentText}" />
    </StackPanel>
</UserControl>
